---
trigger: always_on
---

"Environment: Low RAM (8GB). Do not perform deep repository indexing unless requested. When using the terminal, always use 'cmd /c' for commands. Avoid opening multiple files simultaneously."